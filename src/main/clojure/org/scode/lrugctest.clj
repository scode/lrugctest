(ns org.scode.lrugctest
  (:gen-class)
  (:require [clojure.contrib.duck-streams :as duck-streams])
  (:use compojure.core
        compojure.response
        ring.adapter.jetty))

(defn -main [& args]
  (println "todo"))


